# StockService Deployment and Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stock-service
  namespace: eshopflix
  labels:
    app: stock-service
    app.kubernetes.io/name: stock-service
    app.kubernetes.io/part-of: eshopflix-microservices
spec:
  replicas: 2
  selector:
    matchLabels:
      app: stock-service
  template:
    metadata:
      labels:
        app: stock-service
    spec:
      containers:
      - name: stock-service
        image: ${ACR_NAME}.azurecr.io/eshopflix/stock-service:latest
        ports:
        - containerPort: 8080
        env:
        - name: ASPNETCORE_URLS
          value: "http://+:8080"
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            secretKeyRef:
              name: eshopflix-secrets
              key: ConnectionStrings__StockDb
        envFrom:
        - configMapRef:
            name: eshopflix-shared-config
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: stock-service
  namespace: eshopflix
  labels:
    app: stock-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: stock-service
